/**
 * @description This ruleset allows public read access to slope calculations while restricting write access to authenticated users.
 * All users, including anonymous ones, can create, update, and delete slope calculation documents.
 * @dataStructure Data is stored in a top-level collection called `slope_calculations`.
 * Each document represents a single slope calculation.
 * @keySecurityDecisions Read access is public to allow for easy sharing and demonstration of slope diagrams. Write access is restricted to authenticated users.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows anyone to read slope calculations, but restricts write access to authenticated users.
     * @path /slope_calculations/{slopeCalculationId}
     * @allow (get, list) Allow any user to read the slope calculation.
     * @deny (create, update, delete) Deny unauthenticated users from writing slope calculations.
     * @principle Allows public read access for demonstration purposes, restricts write access for security.
     */
    match /slope_calculations/{slopeCalculationId} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource.data.id == request.resource.data.id;
      allow delete: if isSignedIn() && resource != null;
    }

    function isSignedIn() {
      return request.auth != null;
    }
  }
}